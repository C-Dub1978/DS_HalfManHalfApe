# @angular/build's peerOptional vitest ^3.x (its experimental unit-test builder,
# unused here — ng test runs on karma) conflicts with the vitest ^4.x that
# @storybook/addon-vitest requires. legacy-peer-deps avoids a false ERESOLVE.
legacy-peer-deps=true
